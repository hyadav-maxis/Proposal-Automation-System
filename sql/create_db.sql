-- Create database for Proposal Automation (run once)
-- Usage: psql -U postgres -f create_db.sql
-- If proposal_db already exists, you'll see an error; then run: psql -U postgres -d proposal_db -f database_schema.sql

CREATE DATABASE proposal_db;
